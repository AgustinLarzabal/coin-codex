# Start CLI-First

CoinCodex will expose MVP ingestion operations through CLI commands and a worker process rather than an HTTP API or admin UI. This lets the crawler pipeline prove crawl run creation, queue processing, status inspection, retries, extraction, acceptance, and image downloading before adding product-facing controls.
