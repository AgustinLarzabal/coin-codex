# Prefer Deterministic Extraction for Identity

CoinCodex will extract core Coin Identity fields with deterministic selectors, URL patterns, and parser logic before using AI or provider-level structured extraction. AI extraction can support enrichment and messy descriptions, but identity fields must remain testable and predictable enough to drive acceptance rules.
