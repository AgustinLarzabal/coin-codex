# Use TypeScript on Node.js

CoinCodex will build the ingestion system in TypeScript on Node.js. This aligns with Firecrawl SDK usage, Drizzle, CLI/admin workflows, queue workers, and future integration with TypeScript applications such as Coin Archive.
